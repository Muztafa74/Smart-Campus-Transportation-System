# Vite reads this at build/dev time. Vercel: set VITE_API_URL under Project → Environment Variables.

# Production (must end with /api, or use origin only — see normalizeApiBaseUrl in src/api/client.js)
VITE_API_URL=https://smart-campus-transportation-system.onrender.com/api

# Local backend
# VITE_API_URL=http://127.0.0.1:5000/api
