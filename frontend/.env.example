# Copy to .env.local (Vercel: set in Project → Settings → Environment Variables)

# Local backend
# VITE_API_URL=http://127.0.0.1:5000/api

# Production: your deployed API base (must end with /api — same path prefix as Express mounts)
# VITE_API_URL=https://your-backend.onrender.com/api
